# Net-Pulse
NetPulse is a Python-based network monitoring tool that checks website connectivity, resolves IP addresses, measures TCP connection latency, and continuously monitors the connection status in real time.
